# v0.1.5 — Alpha (doar facturare)

Canal **alpha** = test pentru **document informativ** (factură neoficială). Restul funcțiilor sunt în **Stable v0.1.1**.

## Env

```env
NEXT_PUBLIC_RELEASE_CHANNEL=alpha
```

Badge în admin: „v0.1.5 · Facturare (Alpha)”.

## Ce e în alpha

- `/admin/bookings/[id]/factura` — prezentare pentru client (fără valoare fiscală)
- Link vizibil doar când `RELEASE_CHANNEL=alpha`

## Ce NU mai e doar în alpha

Dashboard Azi, comparativ statistici, Gantt, preț clădire, Realtime, **istoric acțiuni** → rulează pe **Stable** (migrări `006` + `007`).

## Promovare facturare la Stable

După test: scoate gate-ul din `src/lib/features.ts` / pagini factură și promovează în `v0.1.2` stable.
