# v0.1.1 — Stable

Versiunea de producție și pilot client. **Nu** include facturarea neoficială (aceea rămâne în alpha v0.1.5).

## Migrări Supabase

Rulează în ordine (dacă nu le ai deja):

1. `001` … `005` — schema de bază + statistici
2. `006_v015_pricing_realtime.sql` — preț implicit clădire + Realtime pe `bookings`
3. **`007_admin_activity_log.sql`** — istoric acțiuni admin

## Funcții Stable

- Panou admin (HUD), dashboard Azi, comparativ lună vs an trecut
- Calendar Gantt cu mutare drag, disponibilitate, statistici
- Preț implicit per clădire + Realtime / auto-refresh 50s
- **Istoric acțiuni** — `/admin/istoric` + secțiune pe fiecare rezervare
- Cereri, camere, clădiri, setări

## Env

```env
# Stable (implicit) — nu seta alpha
# NEXT_PUBLIC_RELEASE_CHANNEL=stable
```

Sau omit complet variabila `NEXT_PUBLIC_RELEASE_CHANNEL`.

## Jurnal acțiuni

Vezi `docs/istoric-actiuni.md`.
