# =============================================================================
# DEV: contul TĂU Supabase (personal) — OK până răspunde clientul
# STAGING live (2+ săpt., prieten): vezi .env.staging.example + docs/mediu-test-live.md
# PROD: proiect Supabase nou pe office@casaemil.ro + migrare date
# =============================================================================

NEXT_PUBLIC_SUPABASE_URL=https://XXXX.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin Faza A (Server Actions) — Settings → API → service_role (secret!)
# NU pune în GitHub. NU folosi în cod care rulează în browser.
SUPABASE_SERVICE_ROLE_KEY=

# Viitor: n8n pe VPS
# N8N_WEBHOOK_NEW_REQUEST=
# N8N_WEBHOOK_CONFIRMED=

# Pensiune pilot (nu e secret — doar slug)
NEXT_PUBLIC_PENSION_NAME=Casa Emil

# Stable (prod): omit variabila → v0.1.1 + istoric + tot admin
# Alpha (staging): doar pentru test factură neoficială v0.1.5
# NEXT_PUBLIC_RELEASE_CHANNEL=alpha

# Reset factory în Setări admin (șterge clădiri, camere, rezervări). DOAR dev/staging!
# ADMIN_FACTORY_RESET_ENABLED=true
