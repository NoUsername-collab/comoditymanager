/**
 * Single entry for check-in wizard/modal styles (~2.3k lines).
 * Settings layout imports admin-checkin.css directly (server layouts do not
 * keep CSS pulled through this TS barrel). Client check-in UI uses this file.
 */
import "@/styles/features/admin/admin-checkin.css";
