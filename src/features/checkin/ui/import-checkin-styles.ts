/**
 * Single entry for check-in UI styles (~2.3k lines).
 * Import from CheckinModal, CheckinWizardLauncher, or the settings layout —
 * not the CSS file directly. Fiscal / booking / email settings reuse the
 * same row, toggle, and summary classes.
 */
import "@/styles/features/admin/admin-checkin.css";
